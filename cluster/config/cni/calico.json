{
    "name": "calico-k8s-network",
    "type": "calico",
    "etcd_endpoints": "${etcd_urls}",
    "log_level": "debug",
    "ipam": {
        "type": "host-local",
        "subnet": "usePodCidr"
    },
    "kubernetes": {
        "kubeconfig": "/etc/kubernetes/secrets/calico.kubeconfig"
    }
}
